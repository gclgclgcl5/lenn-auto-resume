name = "ai-resume-generator"
compatibility_date = "2024-01-01"
pages_build_output_dir = ".vercel/output/static"

# 注意：环境变量应该在 Cloudflare Pages 控制台中配置
# 不要在这里硬编码敏感信息
# [env.production.vars]
# DEEPSEEK_API_KEY = "your_key_here"

compatibility_flags = ["nodejs_compat"]
